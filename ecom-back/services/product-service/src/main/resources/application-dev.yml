spring:
  data:
    mongodb:
      uri: ${PRODUCT_MONGODB_URI:mongodb://127.0.0.1:27018/ecom_product}
  kafka:
    bootstrap-servers: localhost:9092

management:
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans
